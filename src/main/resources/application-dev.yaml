server:
  port: 8081
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/hyperprof?serverTimezone=UTC
  jpa:
    show-sql: true
    properties:
      hibernate:
        "[format_sql]": true

